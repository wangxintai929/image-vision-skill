---
name: image-vision
description: 当用户上传、粘贴或引用图片（截图、照片、报错截图、图片文件路径等）且当前模型无法直接查看图片内容时使用。用于调用任意 OpenAI 兼容的视觉模型识别图片内容。仅在用户消息涉及图片时使用，不要用于纯文本问题。
---

# 图片识别代理（image-vision）

当前模型无法直接"看到"图片内容时，通过视觉代理脚本调用外部 OpenAI 兼容视觉模型完成识别。

## 使用时机

仅当满足**以下全部条件**时使用：

1. 用户消息包含图片：上传/粘贴的图片、截图、报错截图、照片，或 `@` 引用的图片文件路径（.png / .jpg / .jpeg / .webp / .gif 等）
2. 当前模型无法直接查看图片内容（非多模态模型，或图片未能作为附件送达模型）
3. 用户希望基于图片内容获得回答

## 操作步骤

1. **定位图片路径**：从用户消息或附件中确定图片文件的绝对路径。若只有文件名或不确定位置，先用搜索工具（如 glob）在工作目录中查找；粘贴的截图通常保存于系统临时目录。
2. **定位脚本**：`vision.py` 与 `SKILL.md` 位于同一目录。常见位置：`~/.config/image-vision/`（手动/脚本安装，Windows 为 `%USERPROFILE%\.config\image-vision\`），或 opencode 远程加载时的 skill 缓存目录（以 skill 工具给出的文件列表路径为准）。若找不到，用搜索工具查找 `vision.py`。
3. **执行视觉代理脚本**：

   ```bash
   python <vision.py 所在目录>/vision.py "<图片路径>" -q "<用户问题>"
   ```

   - 支持多张图片：在脚本后依次列出多个路径
   - 用户问题建议原样传入；若用户未提问题，可省略 `-q`，脚本使用默认问题"请详细描述这些图片的内容"
4. **整合回答**：脚本 stdout 输出即图片识别文本。将其作为图片内容，结合用户问题给出完整回答。
5. **配置缺失时**：脚本会提示缺少 `base_url` / `api_key` / `model` 等配置。此时不要编造图片内容，应告知用户需要先复制 `config.example.json` 为 `config.json` 并填写配置（详见 install.md），或设置环境变量 `VISION_BASE_URL` / `VISION_API_KEY` / `VISION_MODEL` / `VISION_CONFIG`。

## 注意事项

- 脚本输出为纯文本，stdout 即模型回答，不要加工改写为其他格式
- API 调用失败（网络、鉴权、超限）时，脚本会把具体错误信息打印到 stderr，照实转述给用户即可
- 不要臆测图片内容；一切以脚本返回结果为准
