---
name: image-vision
description: 当用户上传、粘贴或引用图片（截图、照片、报错截图、图片文件路径等）且当前模型无法直接查看图片内容时使用。用于调用任意 OpenAI 兼容的视觉模型识别图片内容。仅在用户消息涉及图片时使用，不要用于纯文本问题。
---

# 图片识别代理（image-vision）

当前模型无法直接"看到"图片内容时，通过视觉代理脚本调用外部 OpenAI 兼容视觉模型完成识别。

## 使用时机

仅当满足**以下全部条件**时使用：

1. 用户消息包含图片：上传/粘贴的图片、截图、报错截图、照片，或 `@` 引用的图片文件路径（.png / .jpg / .jpeg / .webp / .gif 等）
2. 当前模型无法直接查看图片内容（非多模态模型，或图片未能作为附件送达模型）
3. 用户希望基于图片内容获得回答

## 操作步骤

1. **定位图片路径**：从用户消息或附件中确定图片文件的绝对路径。若只有文件名或不确定位置，先用搜索工具（如 glob）在工作目录中查找；粘贴的截图通常保存于系统临时目录。
2. **禁止直接读取图片**：绝对不要尝试用 read、编辑等工具打开图片文件本身——当前模型不支持图片输入，opencode 会拒绝并报错（`this model does not support image input`）。图片内容一律通过视觉代理脚本获取。
3. **定位脚本**：`vision.py` 与 `SKILL.md` 位于同一目录。常见位置：`~/.config/image-vision/`（手动/脚本安装，Windows 为 `%USERPROFILE%\.config\image-vision\`），或 opencode 远程加载时的 skill 缓存目录（以 skill 工具给出的文件列表路径为准）。若找不到，用搜索工具查找 `vision.py`。
4. **执行视觉代理脚本**：

   ```bash
   python <vision.py 所在目录>/vision.py "<图片路径>" -q "<用户问题>"
   ```

   - 支持多张图片：在脚本后依次列出多个路径
   - 用户问题建议原样传入；若用户未提问题，可省略 `-q`，脚本使用默认问题"请详细描述这些图片的内容"
5. **直接回答，保持无感**：脚本 stdout 输出即图片识别文本。直接以结果回答用户，**不要输出"我调用视觉代理脚本""当前模型无法直接查看图片"之类的解释性文字**。仅当脚本本身失败（配置缺失、网络错误）时，才简洁转述错误信息。
6. **配置缺失时**：脚本会提示缺少 `base_url` / `api_key` / `model` 等配置。此时不要编造图片内容，也不要让用户自行去编辑文件——直接按"通过对话配置"的流程发起提问（opencode 用 `question`、Claude Code 用 `AskUserQuestion`，其他平台直接列出三项），收集参数后写入并验证。配置只认 `config.json`，不读取其他环境变量。

## 通过对话配置（无需手动编辑文件）

当需要视觉模型配置时（首次使用、脚本报缺少配置、用户询问如何配置），**主动发起提问**，不要等待用户自行说明：

1. **发起提问**（按平台选择工具）：
   - **opencode**：调用 `question` 工具，一次性提出三个问题，每项均允许用户自定义填写：
     - `base_url`：API 基础地址，如 `https://api.openai.com/v1`
     - `api_key`：API 密钥（`sk-...`）
     - `model`：视觉模型名称，如 `gpt-4o`
   - **Claude Code**：调用 `AskUserQuestion` 工具，同样一次提出上述三个问题，每题启用自由输入（Other）供用户填写。
   - **其他平台（Codex / zcode 等，无填空工具）**：直接在回复中列出上述三项，请用户一次性回复（如"base_url 填 xxx，api_key 填 xxx，model 填 xxx"），然后继续。
2. **确定配置文件路径**（按顺序选第一个可写的）：
   - `~/.config/image-vision/config.json`（Windows 为 `%USERPROFILE%\.config\image-vision\config.json`）
   - 本 skill 目录下的 `config.json`（远程加载场景）
   - 都不存在时：创建 `~/.config/image-vision/` 目录及 `config.json`（字段模板见同目录 `config.example.json`）
3. **写入配置**：用编辑工具将用户填写的 `base_url` / `api_key` / `model` 写入该文件；已存在的其他字段（如 `timeout`）保留不动，用户未提供的字段不改。
4. **验证**：运行 `python <脚本所在目录>/vision.py --check`，输出"配置检查通过"即完成。
5. **告知用户**：API Key 会以明文保存在本机配置文件 `config.json` 中。

## 注意事项

- 脚本输出为纯文本，stdout 即模型回答，不要加工改写为其他格式
- **无感原则**：识别过程对用户不可见即可——不要解释"正在调用脚本/模型不支持图片"；直接给出基于图片的回答
- API 调用失败（网络、鉴权、超限）时，脚本会把具体错误信息打印到 stderr，照实转述给用户即可
- 不要臆测图片内容；一切以脚本返回结果为准
